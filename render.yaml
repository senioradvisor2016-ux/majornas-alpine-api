services:
  - type: web
    name: majornas-alpine-api
    runtime: node
    buildCommand: echo "Ready"
    startCommand: node majora-api.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3850
